# Visual Inspector — common dependencies
# Install with: pip install -r requirements.txt
#
# For Pi add:    pip install -r requirements-pi.txt
# For WSL add:   pip install -r requirements-wsl.txt

# Core vision
opencv-python>=4.8.0
numpy>=1.24.0

# Web UI (Flask + SocketIO for the Carbon-styled operator interface)
Flask>=2.3.0
Flask-SocketIO>=5.3.0
python-engineio>=4.7.0
python-socketio>=5.10.0
simple-websocket>=1.0.0

# Image handling (for sample data generation and bounding-box display)
Pillow>=10.0.0
