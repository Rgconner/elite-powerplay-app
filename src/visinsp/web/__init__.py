"""Web UI: Jinja templates + static assets for the operator interface.

The UI follows the IBM Design Language (Carbon Design System). Carbon's
CSS is loaded from the official IBM CDN; the templates add only a
small per-page CSS layer for custom chrome (e.g. the bounding-box
canvas). All JavaScript is plain ES2018 — no build step required.
"""
