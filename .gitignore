# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
.env

# Node / Frontend
node_modules/
dist/
.DS_Store

# Environment files (never commit real secrets)
.env
*.env.local

# Kubernetes secrets with real values (use k8s/secrets.yaml as the template)
k8s/secrets.local.yaml

# Editor
.vscode/
.idea/
*.swp

# Database
*.db
*.sqlite

# Logs
*.log

# ── Lockfiles are TRACKED, do NOT ignore ────────────────────────────────────
# package-lock.json       (frontend, npm)
# requirements.lock.txt   (backend, pip)
# These are checked in to guarantee byte-for-byte reproducible builds.
# Remove this block only if you have a good reason.


