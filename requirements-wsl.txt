# Visual Inspector — WSL / x86 development dependencies
# Install on WSL AFTER requirements.txt:
#   pip install -r requirements.txt -r requirements-wsl.txt

-r requirements.txt

# Pytest for running the unit tests during local development
pytest>=7.0.0
pytest-cov>=4.1.0

# (No RPi.GPIO on WSL — the mock GPIO backend is used automatically.)
